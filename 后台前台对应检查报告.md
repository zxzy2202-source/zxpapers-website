# 🔍 ZhixinPaper 后台 ↔ 前台对应关系检查报告（更新版）

> 初版：2026-05-21 ｜ **本次复核：2026-06-06**
> 范围：5 个后台模块（Hero / Posts / Images / SEO / Inquiry）vs 全站前端
> 复核依据：逐项核对当前代码 + 完整 `npm run build`（101 页全绿）+ `tsc` 通过 + 孤儿 slot 脚本扫描

---

## ✅ 检查结论 (TL;DR)

| 模块 | 05-21 旧状态 | **06-06 现状** | 严重 BUG |
|------|------|------|------|
| **Hero 首页** | 🟢 100% | 🟢 100% | 0 |
| **询盘表单** | 🟢 100% | 🟢 100% | 0 |
| **SEO** | 🟡 60%（GA/GTM 没装）| 🟢 ~100% | 0（已修） |
| **博客 / 资源页** | 🔴 50%（resources 脱节）| 🟢 ~100% | 0（已修） |
| **图片管理** | 🔴 40%（slot 大量错位）| 🟢 ~100% | 0（已修） |

**总体评价**：05-21 报告中的 3 个 🔴/🟡 BUG **全部已修复**。五大模块后台↔前台现已完全打通，无遗留 BUG。

---

## 一、Hero 首页 — 🟢 完美对应（持平）
后台 `/admin/hero` 9 字段 → 首页 [PageHero](src/components/shared/PageHero.tsx) 全量读取（badge/eyebrow/title/subtitle/trustBadges/双 CTA/banners[]）。后台改 → 首页生效。

## 二、询盘表单 — 🟢 完美对应（持平）
`<InquiryForm/>` 广覆盖（contact / 所有 markets 国家页 / products / oem / resources 等）→ `/api/inquiry` → KV → 多渠道通知 → 后台 `/admin/inquiries` 查看。
> 06-06 附带无障碍增强：可见标签 + `aria-invalid`/`aria-describedby` + `role="alert"` 错误播报。

## 三、SEO — 🟢 已修复（原 🔴 BUG #1）
| 字段 | 状态 |
|------|------|
| siteTitle / siteDescription / siteKeywords / ogImage | ✅ |
| google / baidu / bing 验证码 | ✅ |
| **googleAnalyticsId / googleTagManagerId** | ✅ **已装**（[layout.tsx:201-241](src/app/layout.tsx#L201) 按后台值条件加载 GA + GTM head/noscript） |
| **siteTitleTemplate** | ✅ **已接**（06-06 修：[layout.tsx](src/app/layout.tsx) 读取 + `%s` 保护；[SeoClient.tsx](src/app/admin/seo/SeoClient.tsx) 新增可编辑输入框 + 校验） |

## 四、博客 / 资源页 — 🟢 已修复（原 🔴 BUG #2）
分类基建：[postsCategories.ts](src/lib/postsCategories.ts) 4 分类 + [postsStore.ts](src/lib/postsStore.ts) `category` 字段 + `getPublishedPostsByCategory()`。后台 [PostEditor](src/components/admin/PostEditor.tsx#L350) 可选分类。

| 资源子页 | 接入方式 | 状态 |
|---------|---------|------|
| `/resources/industry-insights` | 直接调 `getPublishedPostsByCategory` | ✅ |
| `/resources/application-cases` | `<LatestArticles category="application-cases"/>` | ✅ |
| `/resources/oem-guide` | `<LatestArticles category="oem-guide"/>` | ✅ |
| `/resources/product-knowledge` | `<LatestArticles category="product-knowledge"/>` | ✅ |

架构：每页 = 顶部策划长文（硬编码 SEO 内容）+ 底部「Latest Articles」动态区（后台驱动，[LatestArticles.tsx](src/components/shared/LatestArticles.tsx)，无文章则不渲染）。后台发某分类文章 → 自动出现在对应 resources 页 + `/blog`。

## 五、图片管理 — 🟢 已修复（原 🔴 最大坑）
根因从架构层根治：
- [imageSlots.ts:188](src/config/imageSlots.ts#L188) `SlotKey = (typeof IMAGE_SLOTS)[number]["slot"]` + `as const satisfies` → slot 类型从注册表派生
- [imageSlotUtils.ts](src/lib/imageSlotUtils.ts) `getSlotImage(slot: SlotKey, …)` → 拼错/未注册**编译报错**

| 一致性方向 | 保障 | 状态 |
|-----------|------|------|
| 代码 → 注册表（用了没注册） | 编译期强制 | ✅ 不可能发生 |
| 注册表 → 代码（注册没用） | 06-06 脚本扫描：128 注册 / 128 引用 | ✅ **0 孤儿** |

旧报告点名「完全没接 slot」的页面（ghana/kenya/nigeria、oem 三子页、can/detergent 的 blank+custom）现已全部接通（如 [ghana:130](src/app/markets/africa/ghana/page.tsx#L130)）。54 个页面接入 slot。

---

## 六、历史 BUG 修复台账

| # | 旧报告 BUG | 状态 | 证据 |
|---|-----------|------|------|
| 1 | GA/GTM 完全没生效 | ✅ 已修 | layout.tsx:201-241 |
| 2 | Resources 与 posts 脱节 | ✅ 已修 | 4 页全接通 + 分类基建 |
| 3 | IMAGE_SLOTS 注册表与代码错位 | ✅ 已修 | SlotKey union + 0 孤儿 |
| P3 | siteTitleTemplate 写死 | ✅ 已修（06-06） | layout + SeoClient 双向 |

**当前无已知遗留项。**

---

_初版检查：2026-05-21 ｜ 复核 & 修复：2026-06-06_
